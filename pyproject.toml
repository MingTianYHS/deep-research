[project]
name = "codex-deep-research-skill"
version = "0.1.0"
description = "Lightweight persistent deep-research skill for OpenAI Codex"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
