[project]
name = "codex-deep-research-skill"
version = "0.8.0rc1"
description = "Lightweight citation-first deep-research skill for OpenAI Codex"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
