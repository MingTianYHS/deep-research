[project]
name = "codex-deep-research-skill"
version = "1.0.0rc1"
description = "Persistent citation-first research assistant for OpenAI Codex"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
